<%= form_with(model: description, local: true) do |form| %>
  <% if description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(desc.errors.count, "error") %> prohibited this desc from being saved:</h2>

      <ul>
        <% description.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="disabledTextInput">Description</label>
    <%= form.text_field :description, class: 'form-control', placeholder: 'e.g. watercolor', required: true %>
  </div>

  <%= form.submit 'Add', class: 'btn btn-primary', type: 'submit' %>
<% end %>